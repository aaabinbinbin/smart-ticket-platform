<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartticket.domain.mapper.TicketGroupMapper">

    <insert id="insert" parameterType="com.smartticket.domain.entity.TicketGroup"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ticket_group (
            group_name, group_code, owner_user_id, enabled
        )
        VALUES (
            #{groupName}, #{groupCode}, #{ownerUserId}, #{enabled}
        )
    </insert>

    <select id="findById" resultType="com.smartticket.domain.entity.TicketGroup">
        SELECT *
        FROM ticket_group
        WHERE id = #{id}
    </select>

    <select id="findByCode" resultType="com.smartticket.domain.entity.TicketGroup">
        SELECT *
        FROM ticket_group
        WHERE group_code = #{groupCode}
    </select>

    <select id="page" resultType="com.smartticket.domain.entity.TicketGroup">
        SELECT *
        FROM ticket_group
        WHERE 1 = 1
        <if test="enabled != null">AND enabled = #{enabled}</if>
        <if test="keyword != null and keyword != ''">
            AND (group_name LIKE CONCAT('%', #{keyword}, '%') OR group_code LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(1)
        FROM ticket_group
        WHERE 1 = 1
        <if test="enabled != null">AND enabled = #{enabled}</if>
        <if test="keyword != null and keyword != ''">
            AND (group_name LIKE CONCAT('%', #{keyword}, '%') OR group_code LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </select>

    <update id="update" parameterType="com.smartticket.domain.entity.TicketGroup">
        UPDATE ticket_group
        SET group_name = #{groupName},
            owner_user_id = #{ownerUserId},
            enabled = #{enabled}
        WHERE id = #{id}
    </update>

    <update id="updateEnabled">
        UPDATE ticket_group
        SET enabled = #{enabled}
        WHERE id = #{id}
    </update>
</mapper>
