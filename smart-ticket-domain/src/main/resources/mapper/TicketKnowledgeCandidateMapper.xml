<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartticket.domain.mapper.TicketKnowledgeCandidateMapper">

    <insert id="insert" parameterType="com.smartticket.domain.entity.TicketKnowledgeCandidate"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ticket_knowledge_candidate (
            ticket_id, status, quality_score, decision, reason, review_comment, sensitive_risk, reviewed_at, reviewed_by
        )
        VALUES (
            #{ticketId}, #{status}, #{qualityScore}, #{decision}, #{reason}, #{reviewComment}, #{sensitiveRisk}, #{reviewedAt}, #{reviewedBy}
        )
    </insert>

    <update id="update" parameterType="com.smartticket.domain.entity.TicketKnowledgeCandidate">
        UPDATE ticket_knowledge_candidate
        SET status = #{status},
            quality_score = #{qualityScore},
            decision = #{decision},
            reason = #{reason},
            review_comment = #{reviewComment},
            sensitive_risk = #{sensitiveRisk},
            reviewed_at = #{reviewedAt},
            reviewed_by = #{reviewedBy}
        WHERE id = #{id}
    </update>

    <select id="findById" resultType="com.smartticket.domain.entity.TicketKnowledgeCandidate">
        SELECT *
        FROM ticket_knowledge_candidate
        WHERE id = #{id}
    </select>

    <select id="findByTicketId" resultType="com.smartticket.domain.entity.TicketKnowledgeCandidate">
        SELECT *
        FROM ticket_knowledge_candidate
        WHERE ticket_id = #{ticketId}
    </select>

    <select id="findByStatus" resultType="com.smartticket.domain.entity.TicketKnowledgeCandidate">
        SELECT *
        FROM ticket_knowledge_candidate
        WHERE status = #{status}
        ORDER BY updated_at DESC
        LIMIT #{limit}
    </select>
</mapper>
