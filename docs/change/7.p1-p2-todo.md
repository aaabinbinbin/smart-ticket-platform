# P1/P2 TODO 清单

更新时间：2026-04-20

## P1 TODO

- 工单组和队列：
  - 已完成 domain entity、mapper、repository、biz service。
  - 已完成管理端 API 和 ADMIN 写权限。
  - 已完成工单与队列绑定接口。
  - 待补接口级集成测试。
- SLA：
  - 已完成 SLA 策略配置接口。
  - 已完成创建/分配工单时生成或刷新 SLA 实例。
  - 已定义首次响应和解决时限的基础计算口径。
  - 待补定时扫描或事件驱动的违约判断。
  - 待补升级策略和通知策略。
- 自动分派：
  - 已完成规则优先级和冲突处理的第一版：按权重、匹配具体度、ID 倒序选择。
  - 已完成 preview。
  - 已完成真实分派最小闭环：命中目标处理人后复用现有分配服务。
  - 已支持命中目标队列时先绑定工单队列再执行分派。
  - 待明确“只命中组/队列但没有目标处理人”的业务处理方式。
  - 待补 API 集成测试。
- RAG 增强：
  - 建立检索评估样本。
  - 决定是否实现 query rewrite 独立服务。
  - PGvector 独立数据源方案确认后再去除 MySQL JSON 向量兜底。

## P2 TODO

- 审批：
  - 明确哪些工单操作需要审批。
  - 定义审批状态机。
  - 审批结果必须回写到 biz 主流程。
- 子任务：
  - 明确子任务和父工单状态的联动规则。
  - 补子任务权限边界。
- 知识治理：
  - 知识审核通过后再允许进入向量库。
  - 定义知识版本回滚策略。
  - 定义知识过期和下线策略。
- 指标报表：
  - 明确指标口径和统计周期。
  - 先做离线聚合，再考虑实时看板。
- 租户隔离：
  - 明确租户字段注入位置。
  - 明确用户、角色、工单、知识、向量数据的隔离策略。

## 不在当前阶段实现

- 多 Agent 编排。
- MCP。
- 长期记忆。
- 复杂审批流引擎。
- 自动闭环处理工单。
- 用模型替代 biz 层规则判断。
## 2026-04-20 SLA TODO 更新

- 已完成手动 SLA 违约扫描接口：`POST /api/ticket-sla-instances/breach-scan`。
- 仍待实现定时调度入口、升级策略、通知策略和扫描审计记录。
- 当前扫描只处理解决截止时间 `resolve_deadline`，首次响应违约拆分留到后续阶段。
