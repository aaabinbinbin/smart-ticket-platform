spring:
  application:
    name: smart-ticket-platform
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/smart_ticket_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 192.168.100.128
      port: 6379
      password:
      database: 1
      timeout: 3s
  ai:
    model:
      chat: ${SPRING_AI_MODEL_CHAT:none}
      embedding: ${SPRING_AI_MODEL_EMBEDDING:none}
    openai:
      api-key: ${MY_API_KEY:}
      base-url: ${MY_BASE_URL:https://api.openai.com}
      chat:
        options:
          model: ${OPENAI_CHAT_MODEL:kimi-k2.5}
          temperature: ${OPENAI_CHAT_TEMPERATURE:0.1}
      embedding:
        options:
          model: ${OPENAI_EMBEDDING_MODEL:multimodal-embedding-v1}
    vectorstore:
      pgvector:
        # 启用 PGvector 主检索路径时，需要同时开启 SMART_TICKET_AI_VECTOR_STORE_ENABLED
        # 并保证下面 smart-ticket.ai.pgvector.* 指向可用的 PostgreSQL + pgvector 实例。
        enabled: ${SMART_TICKET_AI_VECTOR_STORE_ENABLED:false}
        initialize-schema: false
        schema-name: ${PGVECTOR_SCHEMA:public}
        table-name: ${PGVECTOR_TABLE:vector_store}
        index-type: HNSW
        distance-type: COSINE_DISTANCE
        dimensions: ${PGVECTOR_DIMENSIONS:1536}
        max-document-batch-size: 1000

mybatis:
  mapper-locations: classpath*:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true

smart-ticket:
  ai:
    chat:
      enabled: ${SMART_TICKET_AI_CHAT_ENABLED:false}
    embedding:
      enabled: ${SMART_TICKET_AI_EMBEDDING_ENABLED:false}
    vector-store:
      # false 时 RetrievalService 会明确退回 MySQL fallback，仅作为开发兜底。
      enabled: ${SMART_TICKET_AI_VECTOR_STORE_ENABLED:false}
    pgvector:
      # PGvector 主路径示例：postgresql://host:5432/db，并确保目标库已安装 pgvector 扩展。
      url: ${PGVECTOR_DATASOURCE_URL:jdbc:postgresql://192.168.100.128:5432/smart_ticket_vector}
      username: ${PGVECTOR_DATASOURCE_USERNAME:postgres}
      password: ${PGVECTOR_DATASOURCE_PASSWORD:123456}
      driver-class-name: org.postgresql.Driver
  jwt:
    secret: smart-ticket-platform-local-jwt-secret-change-me-please-20260417
    expiration-minutes: 120
